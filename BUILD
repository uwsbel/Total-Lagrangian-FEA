package(default_visibility = ["//visibility:public"])

# Shared filegroups for tests (avoid cross-package `glob(..)` patterns).
filegroup(
    name = "data_meshes",
    srcs = glob(["data/meshes/**"]),
)

filegroup(
    name = "data_utest",
    srcs = glob(["data/utest/**"]),
)
